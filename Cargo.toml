[package]
name = "rust-expohisto"
version = "0.1.0"
edition = "2021"
description = "Allocation-free OpenTelemetry Exponential Histogram"
license = "Apache-2.0"

[features]
default = ["std"]
std = []

[dependencies]
libm = "0.2"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "mapping"
harness = false
